/**
 * Validates API call authorization.
 * @param {object} req - API request object.
 */
export function validateAuthorization(req) {
	return new Promise((resolve, reject) => {
		// console.log(req.headers);
		// return resolve('xd');
	});
}
